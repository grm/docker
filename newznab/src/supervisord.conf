[supervisord]
nodaemon=true

#[program:apache2]
#command=/bin/bash -c "source /etc/apache2/envvars && exec /usr/sbin/apache2 -DFOREGROUND"

#[program:mysqld]
#command=/bin/bash -c "mysqld"

[program:newznab]
command=/bin/bash -c "/newznab.sh"

